# AI 项目工程协作规范

## 核心原则

1. **稳定优先**
   - 生产可用版本优先于功能升级
   - 任何修改不得破坏现有稳定功能
   - 回滚能力优于新特性开发

2. **小步修改**
   - 单次修改控制在最小作用域
   - 避免大规模重构
   - 每次只改一个核心问题

3. **可验证**
   - 所有修改必须提供验证方案
   - 本地验证 → Git 提交 → 部署 → 线上验证
   - 不得跳过验证步骤

4. **可回滚**
   - 每个阶段必须有明确的回滚方式
   - 保留旧版本代码作为兼容基线
   - Git commit 信息必须清晰可追溯

5. **可追踪**
   - 每次任务必须有任务文档
   - 变更必须记录到 CHANGELOG.md
   - 工作状态必须更新到 TASK_STATUS.md

---

## 每次开始工作前必须执行

AI 开始任务前**必须**执行以下检查：

### 1. 读取 AGENTS.md
- 确认理解工程规范
- 确认遵守核心原则

### 2. 查看 git status
- 确认当前分支状态
- 确认无未提交的修改
- 确认无冲突文件

### 3. 查看最近 commit
- `git log --oneline -10`
- 确认最近修改内容
- 确认无未部署的提交

### 4. 查看 TASK_STATUS.md
- 确认当前阶段
- 确认当前任务节点
- 确认未完成事项

### 5. 确认当前任务节点
- 明确任务目标
- 确认修改范围
- 确认风险评估

**禁止**：
- ❌ 不读取历史状态直接修改
- ❌ 重复执行已经完成任务
- ❌ 随意改变任务目标
- ❌ 跳过验证步骤

---

## 标准开发流程

所有修改**必须**遵循以下流程：

```
1. 检查 (Check)
   ├── git status
   ├── git log --oneline -10
   ├── 读取 TASK_STATUS.md
   └── 确认任务节点

2. 备份 (Backup)
   ├── 关键文件备份
   ├── 数据快照
   └── 配置备份

3. 修改 (Modify)
   ├── 最小作用域修改
   ├── 单一目标原则
   └── 保留兼容性

4. 本地验证 (Local Verification)
   ├── 启动 http.server
   ├── 检查 Console 错误
   ├── 验证数据加载
   └── 确认功能正常

5. Git 提交 (Git Commit)
   ├── git add <文件>
   ├── git commit -m "类型：描述"
   └── 确认 commit 信息清晰

6. 部署 (Deploy)
   ├── GitHub Actions 自动构建
   └── 等待部署完成

7. 线上验证 (Online Verification)
   ├── 访问正式域名
   ├── 检查页面显示
   ├── Console 错误检查
   ├── JSON/API 数据验证
   └── 对比本地与线上一致性

8. 生成记录 (Documentation)
   ├── 更新 TASK_STATUS.md
   ├── 更新 CHANGELOG.md
   └── 生成任务报告
```

**禁止**：
- ❌ 跳过本地验证直接部署
- ❌ 跳过线上验证认为任务完成
- ❌ 修改后不生成记录

---

## 大型修改规则

以下情况**必须**分阶段执行：
- 数据结构变化
- 页面重构
- 数据源切换
- API 修改
- 部署调整

### 阶段执行要求：

1. **保留旧版本方案**
   - 旧数据格式保留至少一个周期
   - 旧页面保留降级方案
   - 旧 API 保留兼容层

2. **建立兼容机制**
   - 新增字段不删除旧字段
   - 新接口保留 fallback
   - UI 支持新旧格式

3. **提供回滚方案**
   - Git commit 包含回滚信息
   - 文档明确回滚步骤
   - 验证回滚可行性

4. **单阶段完成后再进入下一阶段**
   - 当前阶段验证通过
   - 生成阶段报告
   - 用户确认后再继续

---

## 修改文件规则

修改前**必须**记录：
- **修改原因**: 为什么需要修改
- **修改文件**: 具体修改哪些文件
- **影响范围**: 影响哪些功能
- **回滚方式**: 如何恢复到修改前

**禁止**：
- ❌ 一次修改大量无关文件
- ❌ 删除已有正常功能
- ❌ 未验证直接替换核心逻辑

---

## Git 管理规则

### 每个重要阶段必须提交

**Commit 格式规范**：

| 类型 | 格式 | 说明 |
|-----|------|------|
| **feat** | `feat: 描述` | 新增功能 |
| **fix** | `fix: 描述` | 问题修复 |
| **refactor** | `refactor: 描述` | 结构调整 |
| **docs** | `docs: 描述` | 文档更新 |
| **chore** | `chore: 描述` | 日常维护 |

**示例**：
```
feat: 新增首页推荐策略 A/B 测试功能
fix: 修复 recommendations.json 加载失败问题
refactor: 调整前端组件结构提升性能
docs: 更新部署流程说明
chore: 每日数据分析更新
```

### 提交前检查：

```bash
git diff
```

**必须确认**：
- ✅ 只提交本任务相关文件
- ✅ 无敏感数据泄露
- ✅ 无未完成的临时文件

---

## 部署规则

### 部署成功不代表完成

**必须验证**：
1. **正式域名**
   - 访问 `https://500wan.mootlsv.com/`
   - 确认 CDN 生效

2. **页面显示**
   - 检查布局正常
   - 检查数据渲染
   - 检查交互功能

3. **Console 错误**
   - 打开浏览器 Console
   - 检查 JS 报错
   - 检查资源加载

4. **数据加载**
   - 检查 Network 面板
   - 验证 JSON 数据
   - 验证 API 响应

5. **API/JSON 访问**
   - 直接访问数据文件 URL
   - 验证数据格式
   - 验证数据完整性

**禁止**：
- ❌ 只验证 `*.pages.dev` 临时地址
- ❌ 不检查 Console 错误
- ❌ 不验证 JSON 数据完整性

---

## 出现问题处理规则

### 发现异常时

**立即停止**继续修改。

**执行**：

```
1. 问题定位 (Diagnosis)
   ├── 检查错误日志
   ├── 检查网络请求
   └── 检查数据状态

2. 输出诊断报告 (Report)
   ├── 问题现象
   ├── 可能原因
   └── 验证矩阵

3. 确认方案 (Solution)
   ├── 分析根本原因
   ├── 制定修复方案
   └── 评估风险

4. 执行修复 (Fix)
   ├── 小步修改
   ├── 本地验证
   └── 部署验证
```

**禁止**：
- ❌ 连续打补丁
- ❌ 不分析直接修改
- ❌ 跳过诊断步骤

---

## 生产修改强制规则（Phase 10 Recovery R4 新增）

任何对生产环境的修改**必须**严格遵循以下 7 步流程，缺一不可：

### 强制流程

1. **创建任务编号**
   - 格式：`Phase NN Task #XX`（如 `Phase 11 Task #41`）
   - 明确变更目标、范围、风险评估

2. **修改前记录 baseline**
   - 记录当前 Git HEAD、生产 URL 状态、关键资源 HTTP 状态
   - 确认回滚路径（标签或 commit）

3. **修改后本地验证**
   - 启动 `python -m http.server` 本地验证
   - 检查 Console 无错误、数据加载正常、功能无退化

4. **Git commit**
   - 使用规范 commit message（feat/fix/refactor/docs/chore）
   - 仅提交本任务相关文件，禁止批量无关修改

5. **Cloudflare 部署成功**
   - GitHub Actions 自动部署
   - **确认 workflow 状态为 success，且 `wrangler pages deploy` 步骤已实际执行**
   - 若"提交更新"步骤 `git push` 失败，部署步骤会被跳过，必须重新触发

6. **正式域名验证**
   - 访问 `https://500wan.mootlsv.com/`
   - 确认页面、JS、JSON、Console 全部正常
   - **仅验证 `*.pages.dev` 临时地址无效**

7. **更新 CHANGELOG**
   - 记录问题 / 原因 / 修复 / 验证
   - 同步更新 `TASK_STATUS.md` 状态

### 禁止项

- ❌ **禁止未验证直接部署**（本地验证是部署前置条件）
- ❌ **禁止直接修改生产环境**（所有修改须经 Git + 部署流程）
- ❌ **禁止用缓存解释未确认的问题**（必须实际 curl 验证生产状态）
- ❌ **跳过正式域名验证**（必须验证 `500wan.mootlsv.com`）

### 当前冻结状态

- **基线版本**: `production-stable-v1.0`（指向 `36d9bfc`）
- **状态**: 🟢 已冻结，只读维护
- **禁止**: 开发新功能 / 优化代码 / 修改首页 / 调整 UI
- **解锁条件**: 用户明确下一阶段开发指令

---

## 附录

### 常用命令

```bash
# 查看当前状态
git status

# 查看最近提交
git log --oneline -10

# 查看修改内容
git diff

# 备份文件
cp file.js file.js.bak

# 本地测试
python -m http.server 8888

# 回滚到指定版本
git checkout <commit> -- <file>
```

### 规范版本

- **版本**: v1.0
- **生效日期**: 2026-08-22
- **维护**: 所有 AI 助手

---

**重要提示**:
本规范是项目工程协作的基础规则。
所有 AI 辅助开发**必须**严格遵守。
违反核心原则可能导致工程质量问题。

**遵守规范 = 稳定 + 安全 + 可维护**
